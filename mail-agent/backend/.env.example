PORT=3000
DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5432/mail_agent
# Optionnel: chemin vers le fichier OAuth client Google.
# Si absent, le backend cherche d'abord credentials.json,
# puis un fichier client_secret_*.json dans le dossier backend.
# GOOGLE_CREDENTIALS_PATH=./client_secret_xxxxx.json

# Requis pour l'agent reader (analyse des mails)
# ANTHROPIC_API_KEY=sk-ant-...
